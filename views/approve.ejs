<!DOCTYPE html>
<html lang="ja">

</html>

<head>
    <meta charset="UTF-8">
    <title>承認</title>
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
    <div class="top-wrapper">

        <div class="top-image">
            <h1><a href="/index"><img src="/images/logo.jpg" alt="LexSol"></a></h1>
        </div>
        <h1 class="title">承認</h1>


        <% if (typeof xlsk !== 'undefined') { %>
            <% var projectName = '' %>
            <% for ( row in xlsk ) { %>
                <% if (projectName !== xlsk[row].project) { %>
                    <% projectName = xlsk[row].project %>
                    </table>
                    <h2><%= xlsk[row].project %></h2>
                    <table border="1" width="50%" height="70%">
                        <tr>
                            <th>対応</th>
                            <th>対応者</th>
                            <th>進捗</th>
                            <th>重要度</th>
                            <th>対応予定日</th>
                            <th>完了日</th>
                            <th>工数(人日)</th>
                            <th>種別</th>
                            <th>備考</th>
                        </tr>
                <% } %>
                <tr>
                    <th><%= xlsk[row].task %></th>
                    <th><%= xlsk[row].person %></th>
                    <th><%= xlsk[row].progress %></th>
                    <th><%= xlsk[row].importance %></th>
                    <th><%= xlsk[row].taskDate %></th>
                    <th><%= xlsk[row].compDate %></th>
                    <th><%= xlsk[row].manHour %></th>
                    <th><%= xlsk[row].taskType %></th>
                    <th><%= xlsk[row].note %></th>
                    <th>
                      <a href="/approve?id=<%=xlsk[row].id %>">承認</a>
                   </th>
                   <th>
                       <a href="/approve?id=<%=xlsk[row].id %>">非承認</a>
                   </th>
                </tr>                 
            <% } %>

        </table>
        <% }%>

        <% if (approveMsg) { %>
            <h3><%= approveMsg %></h3>
        <% } %>
        <br>

        <a href="/index" class="button">ホームに戻る</a>
    </div>
</body>

</html>